apiVersion: v1
kind: Service
metadata:
  name: myinsuranceapp-az-service
  labels:
    app: myinsuranceapp-az
spec:
  type: NodePort
  selector:
    app: myinsuranceapp-az-az    
  ports:
  - port: 5000
    targetPort: http
    nodePort: 32410
    protocol: TCP    
    name: http